# Recipe-Recommendation-System
運用 Node.js 與 MySQL 開發的智慧食譜推薦系統，具備符合 BCNF 正規化標準的資料庫架構與會員管理機制

### 概念呈現影片
[![系統展示影片](https://img.youtube.com/vi/pK_yH8D93wc/maxresdefault.jpg)](https://youtu.be/pK_yH8D93wc)

## Overview
透過我們所建立之查詢系統及資料庫解決使用者運用食材上的困難，將冰箱中的食材組合成一道完美的佳餚，並給予使用者食譜教予製作過程[cite: 1]。
**開發流程**：系統需求分析 ➔ E-R圖邏輯設計 ➔ 關聯表轉換與正規化 ➔ 實體建置 (MySQL) ➔ 系統測試與整合[cite: 1]。

### 開發階段核心技術對應檔案
1. **基礎前端介面建置**：實作登入、註冊、查詢與創建介面 ➔ `static/`
2. **後端伺服器設定**：處理前端路由與請求 ➔ `main.py`
3. **資料庫邏輯與模型設計**：正規化關聯表模型 ➔ `database.mwb`
4. **專題成果與報告**：完整書面說明與提報簡報 ➔ `Final_Report.pdf`, `Presentation_Slides.pptx`

---

## 專題簡介
發現許多人會看著冰箱裡滿滿當當的食材卻不知道從何下手，希望可以開發一系統來解決此日常生活中常見的難題[cite: 1]。
本專題不只建立單一的前端網頁，而是從資料庫正規化的基礎出發，依序完成 E-R 圖設計、關聯表轉換，最後透過實體建置與前端介面整合，打造一套結構嚴謹的智慧食譜管家[cite: 1]。

## 系統目標與需求
* 使用者可註冊登錄系統，帳號不可重複[cite: 1]。
* 使用者登錄後可記錄自己的飲食偏好(Ex.葷素)，並設定自己的名稱，日後可更改[cite: 1]。
* 建立完整的食材分類(穀物類、水果類、蔬菜類、豆魚蛋肉類、乳品類、油脂與堅果種子類)[cite: 1]。
* 使用者可建立食譜與他人分享，亦可收藏他人的食譜[cite: 1]。
* 使用者可利用食材查詢食譜，系統需展示食譜製作方式、所需食材與葷素標籤[cite: 1]。

## 資料庫邏輯設計
為了妥善管理使用者、食材與食譜之間的複雜關聯，本系統進行了嚴謹的邏輯設計：
* **核心實體**：使用者 (包含密碼、名稱、飲食偏好)、食材 (包含名稱、營養分類)、食譜 (包含名稱、標籤、製作方式)[cite: 1]。
* **關聯性設計**：設計了查詢、收藏、包含與創建等多對多或一對多的關聯邏輯[cite: 1]。

## 方法

### 1. E-R 圖轉換關聯表
為確保資料結構完整，我們將 E-R 圖明確轉換為以下關聯表：
* `使用者表`、`食材表`、`食譜表`[cite: 1]。
* `查詢表`、`收藏表`、`包含表`、`食譜所需食材表`[cite: 1]。

### 2. 資料庫正規化 (Normalization)
針對轉換後的關聯表，進行了嚴格的正規化檢驗與修正：
* **1NF (第一正規化)**：針對食譜表中「所需食材」可能不只一種的多值屬性，將其拆分成多筆資料（如將奶油、玉米、洋蔥拆解為獨立資料列）以符合 1NF 標準[cite: 1]。
* **2NF (第二正規化)**：確保所有非鍵值屬性皆完成功能相依於主鍵[cite: 1]。
* **3NF (第三正規化)**：確認非鍵值屬性間不存在相依功能[cite: 1]。
* **BCNF**：驗證功能相依決定因素都是候選鍵[cite: 1]。

## 系統實作成果與測試

### MySQL 實體建置
使用 MySQL 語法成功創建 `Users`, `Ingredients`, `Recipes`, `Search`, `Collect`, `Contain`, `Ingredient_of_recipe` 等資料表，並成功寫入測試資料[cite: 1]。

### 前端介面整合
系統涵蓋多個功能完善的互動頁面，實作畫面包含：
* 未登入時主頁與登入、註冊頁面 (含錯誤提示)[cite: 1]。
* 更改名稱介面、搜尋介面與搜尋特定食材的結果清單[cite: 1]。
* 自己創建的食譜管理、特定使用者的食譜瀏覽、以及詳細的食譜製作頁面[cite: 1]。

## 實驗心得與反思
在這次的報告中我們學習到個體間關聯的分析與如何建立屬於自己的 DataBase[cite: 1]。但在實作過程中，我們也進行了深度反思：
* **資料庫架構反思**：分析後發現部分個體間關連不夠完善，例如查詢表格無實質性用途，且「包含表」與「食譜所需食材表」欄位過於相似[cite: 1]。
* **技術挑戰**：在嘗試從前端通過 JavaScript 調用後端 (Node.js+Express+MySQL) 時，無法在同一程式碼中回傳值，因此暫時無法完美結合前後端登入系統[cite: 1]。

## 未來發展
目前已將所構想之網站頁面架設起來[cite: 1]，未來希望能進一步突破技術瓶頸：
1. **前後端深度整合**：繼續嘗試解決回傳值問題，完成前後端資料的無縫串接[cite: 1]。
2. **資料庫擴充**：目前的資料筆數不多，限制了使用者所能查詢的範圍，未來將持續擴充資料庫以提升系統實用性[cite: 1]。

## 使用技術
* **資料庫管理**：MySQL[cite: 1]
* **前端開發**：Html、CSS、JavaScript[cite: 1]
* **後端框架**：Node.js (搭配 Express 嘗試)、Python[cite: 1]

## 個人貢獻
在本次四人團隊專題中，我主要負責/參與了以下核心任務：
* **程式碼實作**：負責前端網頁介面 (HTML/CSS/JS) 的架設與串接邏輯設計[cite: 1]。
* **文件統整**：撰寫並彙整期末書面報告，詳實記錄正規化過程與系統架構[cite: 1]。
* **專案展示**：負責專題的口頭報告，向師長與同學展示系統成果與設計理念[cite: 1]。

## 專題資訊
* **專題名稱**：利用食材查詢對應資料庫中之食譜[cite: 1]
* **專題成員**：黃妤涵、繆語、李佩臻、褚亞誠 (第 10 組)[cite: 1]
